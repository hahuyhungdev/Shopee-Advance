const path = {
  home: '/',
  login: '/login',
  register: '/register',
  profile: '/profile',
  logout: '/logout',
  products: '/products',
  categories: '/categories'
} as const

export default path
